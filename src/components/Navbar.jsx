function Navbar() {
  return (
    <nav>
      <h1>CodeCollab</h1>

      <div>
        <a href="#">Home</a>
        <a href="#">Problems</a>
        <a href="#">Rooms</a>
        <a href="#">Leaderboard</a>
      </div>

      <button>Login</button>
    </nav>
  );
}

export default Navbar;
